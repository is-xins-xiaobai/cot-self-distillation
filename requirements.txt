torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
datasets>=2.14.0
tqdm>=4.66.0
numpy>=1.24.0

# Optional backends
openai>=1.3.0  # For OpenAI API
vllm>=0.2.0  # For local inference
together>=0.2.0  # For Together API

# Training
bitsandbytes>=0.41.0  # For 8-bit training
peft>=0.6.0  # For LoRA (optional)
